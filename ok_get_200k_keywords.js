javascript: function onlyUnique(value, index, self) { return self.indexOf(value) === index;}const getKeyWords = () => {let divsKeyWords = document.querySelectorAll('div[class="vid-card"]');let tagsTextRaw = "";[...divsKeyWords].map(el => {elVws = el.querySelector('div[class="vid-card_views"]').innerText.replace(/\s/g, '');if (parseInt(elVws) > 200000) {tagsTextRaw += " " + el.querySelector('div[class="vid-card_n"]').innerText.replace(/[^a-z\u0400-\u04FF]/gi, " ").trim().replace(/[\s]{1,}/g, ' ');}});let tags_pre = tagsTextRaw.split(" ");let unique = tags_pre.filter(onlyUnique);let tags_text = unique.join(', '); localStorage.setItem('oktag_keywords', JSON.stringify(tags_text));};getKeyWords();
